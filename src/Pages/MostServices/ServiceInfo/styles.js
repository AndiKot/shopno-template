import {createUseStyles} from "react-jss";

export const useStyles = createUseStyles({
    serviceInfo: {
        display: 'inline-block',
        marginTop: 10,
        position: 'relative',
        overflow: 'auto',
    },
    serviceInfoImage: {
        float: 'left',
    },
});
